import java.util.Arrays;

class Solution {
    public int solution(int[] array) {
        Arrays.sort(array);
        int mLength = array.length / 2;
        int answer = array[mLength];
        return answer;
    }
}
